// Package bootstrap provides shared types used across the clusterbox CLI.
package bootstrap

// DefaultK3sVersion is the pinned k3s release used when a k3s version is
// not explicitly specified.
const DefaultK3sVersion = "v1.32.3+k3s1"
