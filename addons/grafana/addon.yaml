name: grafana
version: 5.6.0
description: Grafana Operator — manage Grafana instances, dashboards, and datasources as Kubernetes CRDs.

strategy: helmchart
requires: []

secrets:
  - key: GRAFANA_ADMIN_PASSWORD
    description: Grafana admin password, referenced by the Grafana instance CR
    required: false
